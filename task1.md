# Task 1
1. What classes/interfaces will be in your initial design? Just name them.
- Lot
- Spot
- Vehicle
2. [UML Diagram](task1.png)
3. Spot can have an `size` attribute that has three possible values, `small`, `medium`, and `large`. Vehicle can have a `size` attribute that is of the same type. A vehicle can only park in a `spot` if its `size` attribute matches it, and if the spot is not occupied.